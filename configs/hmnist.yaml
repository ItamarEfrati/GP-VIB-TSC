defaults:
  - _self_
  - datamodule: hmnist
  - model: gpvib_e_cnn1d_d_flatten
  - trainer: hmnist
  - logger: tensorboard
  - callbacks: hmnist
  - hydra: default
  - debug:
  - paths: default
  - metrics: hmnist
  - extras: default

seed:
mode: eval
optimized_metric: ${metrics.optimize}

# evaluate on test set, using best model weights achieved during training
# lightning chooses best weights based on metric specified in checkpoint callback
ckpt_path:

# name of the run, should be used along with experiment mode
datamodule_name: hmnist
model_name: gp_vib
num_runs: 5



